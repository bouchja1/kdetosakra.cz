import { createAction } from 'typesafe-actions';

export const setLastPanoramaPlace = createAction('@result/SET_LAST_PANORAMA_PLACE')();
export const resetLastPanoramaPlace = createAction('@battle/RESET_LAST_PANORAMA_PLACE')();
